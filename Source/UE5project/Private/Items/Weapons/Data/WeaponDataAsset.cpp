// Fill out your copyright notice in the Description page of Project Settings.


#include "Items/Weapons/Data/WeaponDataAsset.h"

/*
FWeaponSetsInfo UWeaponDataAsset::FindWeaponInfo(const FName& WeaponName, bool bLogNotFound) const
{
	const FWeaponSetsInfo* Info = WeaponList.Find(WeaponName);
	if (Info)
	{
		return *Info;
	}

	if (bLogNotFound)
	{
		UE_LOG(LogTemp, Error, TEXT("Not WeaponInfo"))
	}

	return FWeaponSetsInfo();
}
*/